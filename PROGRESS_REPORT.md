# 基于视觉语言模型的多标记情感识别研究 — 进展汇报

**汇报人**：申新卓（模型架构）
**日期**：2026 年 9 月 11 日
**代码仓库**：https://github.com/Moon-shiyue/Research_on_Multi-Label_Emotion_Recognition_Based_on_Visual-Language_Models

---

## 一、总体进度

| 阶段 | 计划时间 | 状态 |
|------|---------|------|
| 一、文献调研、数据筹备、基线搭建 | 2026.04 - 05 | 数据部分进行中（周跃负责） |
| 二、核心模型架构设计与实现 | 2026.06 - 08 | **已完成** |
| 三、损失函数设计、训练与性能验证 | 2026.09 - 10 | 待数据集就绪后开始 |
| 四、可解释性分析与轻量化、Demo | 2026.11 - 2027.01 | 未开始 |
| 五、成果转化与结题 | 2027.02 - 04 | 未开始 |

当前节点：第二阶段的代码与验证工作已交付，处于「等训练数据 → 进入训练验证」的衔接期。

---

## 二、已完成：模型架构与代码

### 2.1 关于「复现」与「改进」的说明

目前仓库中实现的是**同一套 CLIP 基座框架下的两种配置**，可用一个参数切换：

| 配置 | 组成 | 用途 |
|------|------|------|
| **基线配置** | CLIP 双塔 + 层次化注意力融合 + 标签关联（GCN + Label Attention）+ 通用多标记分类头 | 作为对照基准（常规做法的组合） |
| **完整模型** | CLIP 双塔 + 三大核心模块（冲突感知融合 + 情感环形分类头 + VL-Adapter） | 申请书「研究内容」的技术方案 |

另有 `qwenvl_emotion/` 目录，是早期对「Qwen2.5-VL + LoRA 微调」路线的探索实现，
经评估后未纳入技术方案（与三大模块的设计不兼容、显存需求翻倍、可解释性难获取），
已归档为技术路线探索记录，附有选型依据说明。

### 2.2 三大核心模块的实现

**模块① 基于注意力引导的冲突感知跨模态融合**

- 文本情绪注意力子模块：双路径冲突注意力，分别提取字面情感与真实意图特征，
  通过余弦差异度与可学习的动态边界划分「情感一致片段 / 情感冲突片段」
- 视觉情绪注意力子模块：以文本情感特征为引导计算 patch 级相似度，
  筛选核心情感区域，对情感对立区域施加负偏置以抑制语义干扰
- 跨模态冲突感知注意力对齐：对齐权重由冲突程度显式决定（冲突越大权重越低），
  一致时强化特征互补，冲突时保留独立特征以避免错误对齐
- 损失：模态内对比学习（图像-图像、文本-文本）+ 双向三元组排序损失

**模块② 基于情感环形表示的多标记分类头**

- 依据 Mikels Wheel 环形模型，将 8 类基础情感按 `θ_j = (2j-1)/8·π` 分布于环形，
  积极/消极各占半圆
- 三分支输出：极性判别（积极/中性/消极三分类）、角度类型（sigmoid 多标记）、
  强度回归（0~1）
- 三维直角坐标转换：`x = r·a·cosθ, y = r·a·sinθ, z = v`
- 渐进式环形损失：极性约束 → 类型约束 → 强度加权的细粒度约束（由粗到细三步）
- 引入标签共现关联矩阵，增强复合情感的联合表达能力

**模块③ 基于 VL-Adapter 的跨场景情感泛化**

- 在 CLIP 文本/视觉编码器各层插入「降维–激活–升维–残差」结构适配器，
  训练全程冻结预训练主干
- 适配器参数解耦为「共享参数」（建模域不变知识）与「域特定低秩参数」
  （捕捉场景特有分布偏移），支持动态扩展新场景
- 可训练参数占主干约 4.6%，符合申请书「3%~5%」的轻量化目标
- 辅以域分布对齐损失，抑制跨场景特征漂移

### 2.3 方法来源与改进点

三大模块在已有工作基础上完成，具体来源与改进如下：

| 模块 | 借鉴的工作 | 本项目的改进 |
|------|-----------|-------------|
| ① 冲突感知融合 | CMSIS 的模态内语义干扰抑制与三元组排序损失（EAAI 2024）；动态相关性边界设计 | 将模态内干扰抑制迁移至多标记情感任务；提出字面/意图双路径冲突注意力；设计对齐权重随冲突单调递减的跨模态对齐门控 |
| ② 情感环形分类头 | Emotion Circle 三维环形表示与渐进式环形损失（CVPR 2021）；Mikels Wheel 情感模型（2005） | 将连续环形表示改造为适配多标记并行预测的三分支分类头；角度采用「先验角度 + 受限偏移」以保证情感类型可辨识；修正原损失在 2π 边界处的角度误差计算 |
| ③ VL-Adapter | VL-Adapter 的适配器结构与插入位置（CVPR 2022） | 实现适配器参数的共享/域特定解耦，支持跨场景快速适配与域增量扩展 |

> 说明：申请书「研究内容」中的方法描述均写为「借鉴」，与此处的定位一致。

---

## 三、验证情况

代码已完成模块级功能验证，验证脚本与报告均在仓库中：

| 验证脚本 | 覆盖内容 | 结果 |
|---------|---------|------|
| `emotion_model/verify_modules.py` | 三大核心模块 + 集成 | **102 / 102 通过** |
| `emotion_model/verify.py` | 编码器、融合、分类头、标签关联等基础组件 | **69 / 69 通过** |

验证不止于维度检查，还包括关键行为的语义验证，例如：

- 冲突场景下对齐权重确认下降（一致 0.88 vs 冲突 0.12），且该性质由结构保证、不依赖训练收敛
- 单一情感经环形映射后的角度还原误差 < 1e-4 弧度；复合情感角度落在组成情感之间
- 环形角度误差正确处理 2π 周期性（环形误差 0.01 vs 欧氏误差 6.27）
- 适配器梯度仅流入当前场景参数（验证域隔离）；可训练参数占比 4.57%
- 消融实验矩阵 6 组配置均可正常初始化与前向

此外，完整模型的端到端训练流程（训练 → 验证 → 保存 → 加载 → 测试评估）
已用构造数据跑通，确认工程链路可用。

**需要说明的是**：以上均为**模块功能验证**，用于确认实现正确、可运行；
模型在真实数据集上的**性能指标（F1 / mAP / KL / Cosine 等）尚未产出**，
需在第三阶段用真实数据训练后给出。

---

## 四、当前依赖与待办

**依赖**：训练集尚未就绪。三大模块采用 8 类 Mikels 基础情感标签体系，
需要数据集提供对应的标签列（或使用代码中已实现的 `multihot_12_to_8()`
完成从 12 类扩展标签到 8 类基础情感的映射）。

**算力**：完整模型（含 VL-Adapter）约需 8GB 显存；若同时跑 6 组消融配置的
多轮实验，建议申请实验室 GPU 资源或按项目预算租赁云算力。

**待确认**：损失项权重配比（分类损失 : 环形损失 : 对比损失）需在训练阶段调优。

---

## 五、下一步计划（数据就绪后）

1. 跑通基线配置与完整模型，确认训练链路与指标计算无误
2. 完成 6 组消融实验（移除模块① / ② / ③、移除对比损失、基线、完整模型），
   验证各模块贡献
3. 与领域内公开结果（如 CARAT 等多模态多标记情感识别方法）做性能对照
4. 输出训练日志、指标表格与消融分析，供论文与结题报告使用

---

## 附：代码结构

```
emotion_model/                 ← 项目技术方案（CLIP 基座）
├── conflict_fusion.py         模块① 冲突感知跨模态融合
├── circular_head.py           模块② 情感环形表示多标记分类头
├── vl_adapter.py              模块③ VL-Adapter 跨场景泛化
├── base_encoder.py            CLIP 双塔编码器封装
├── full_model.py              完整模型（支持完整/消融配置切换）
├── config.py                  配置（标签体系、超参数、消融配置矩阵）
├── train.py                   训练脚本（--full / --ablation）
├── verify_modules.py          核心模块验证（102 项）
├── verify.py                  基础组件验证（69 项）
├── CORE_MODULES.md            核心模块技术实现说明
├── TECHNICAL_REPORT.md        基础架构技术说明
└── module_verification_report.txt / verification_report.txt  验证报告

qwenvl_emotion/                ← 早期技术路线探索记录（未纳入方案）
```
