# 基础深度学习
torch>=2.1.0
torchvision>=0.16.0
numpy>=1.24.0

# Hugging Face 生态（CLIP / Qwen2.5-VL）
transformers>=4.40.0
peft>=0.10.0
accelerate>=0.30.0
safetensors>=0.4.0
bitsandbytes>=0.43.0   # Qwen-VL 量化（显存不足时可选）

# 图像处理
Pillow>=10.0.0

# 评估指标
scikit-learn>=1.3.0

# 训练工具
tqdm>=4.66.0

# 可选：加速
# flash-attn>=2.5.0   # 需要编译，非必需
